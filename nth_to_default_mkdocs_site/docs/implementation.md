# Python module map

Core responsibilities:

1. Hazard curves (piecewise) + inverse mapping
2. Bootstrap to CDS par spreads
3. Copula sampling (Gaussian / Student-t)
4. Pricing (extract \(\tau_{(k)}\), value legs)
5. Capital (loss distribution, EL/VaR/ES)
