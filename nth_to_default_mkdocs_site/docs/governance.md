# Reproducibility and governance

Capture:

- input spreads (date/source)
- discounting + recovery assumptions
- dependence proxy, window, cleaning method
- simulation settings (paths, seed, copula, \(\nu\))
- outputs tied to a git commit hash
